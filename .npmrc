# Ensure all dependencies are installed (including devDependencies)
production=false

# Use npm for package management
package-lock=true

# Ensure scripts can access dependencies
ignore-scripts=false 